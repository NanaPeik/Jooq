server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/weather?currentSchema=public
    username: postgres
    password: postgres
  flyway:
    baseline-version: 1
    baseline-on-migrate: true
    mixed: true
